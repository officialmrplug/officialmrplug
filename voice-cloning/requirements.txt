# Install torch first with the index URL matching your hardware (see setup.sh):
#   CUDA : pip install torch==2.6.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#   CPU  : pip install torch==2.6.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cpu
#   Apple: pip install torch==2.6.0 torchaudio==2.6.0
torch==2.6.0
torchaudio==2.6.0
chatterbox-tts==0.1.7

# Only needed for server.py (the local HTTP API)
fastapi>=0.115
uvicorn>=0.30
